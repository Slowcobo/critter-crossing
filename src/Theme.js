import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      light: "#52c7b8",
      main: "#009688",
      dark: "#00675b",
    },
    secondary: {
      light: "#fff350",
      main: "#ffc107",
      dark: "#c79100",
    },
  },
});

export default theme;
