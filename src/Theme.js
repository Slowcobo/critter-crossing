import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      light: "#60ac5d",
      main: "#2e7c31",
      dark: "#004f04",
    },
    secondary: {
      light: "#ff9d3f",
      main: "#ef6c00",
      dark: "#b53d00",
    },
  },
});

export default theme;
